apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: wow-world-route
  namespace: games
spec:
  parentRefs:
    - name: envoy-internal
      namespace: networking
      sectionName: wow-world
  rules:
    - backendRefs:
        - name: ac-worldserver
          port: 8085
