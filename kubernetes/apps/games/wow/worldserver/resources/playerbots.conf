# Overrides for AzerothCore's playerbots.conf.dist.
# Only settings that differ from the module's shipped defaults need to be
# listed here; anything omitted falls back to playerbots.conf.dist.
# Full reference: https://github.com/mod-playerbots/mod-playerbots/wiki/Playerbot-Configuration

# Enable the playerbots module
AiPlayerbot.Enabled = 1

# Enable randombots so the world feels populated without manual setup
AiPlayerbot.RandomBotAutologin = 1

# Start conservative; raise once resource usage at this count is confirmed
# stable. Each bot adds meaningful CPU/DB load.
AiPlayerbot.MinRandomBots = 50
AiPlayerbot.MaxRandomBots = 200

# Keep bot accounts easily identifiable/manageable
AiPlayerbot.RandomBotAccountPrefix = "rndbot"

# Recommended by upstream docs: roughly (CPU cores - 2), 4-6 for most
# systems. This belongs in worldserver.conf, not playerbots.conf, but is
# noted here as a reminder since it's the single most impactful setting
# for playerbot performance. Set MapUpdate.Threads accordingly if a custom
# worldserver.conf is added later.
